# 📋 Полная инструкция по копированию проекта Hotel Concierge

## Часть 1: Экспорт из текущего проекта

### 1.1. Сохраните код через GitHub
1. В редакторе poehali.dev: **Скачать → Подключить GitHub**
2. Войдите в GitHub и дайте права доступа
3. Выберите аккаунт/организацию
4. Код автоматически сохранится в новый репозиторий

### 1.2. Запишите секреты
Вам понадобится только один секрет:
- `DEEPSEEK_API_KEY` - ваш API ключ от DeepSeek

Остальные секреты (DATABASE_URL, AWS_*) создаются автоматически в новом проекте.

---

## Часть 2: Создание нового проекта

### 2.1. Создайте новый проект
1. Откройте https://poehali.dev
2. Нажмите **"Создать новый проект"**
3. Назовите проект (например: "Hotel Concierge - Копия")

### 2.2. Импортируйте код
**Вариант А: Через GitHub (рекомендуется)**
1. В новом проекте: **Скачать → Подключить GitHub**
2. Выберите репозиторий из шага 1.1
3. Код автоматически загрузится

**Вариант Б: Скачать ZIP**
1. В старом проекте: **Скачать → Скачать код**
2. Распакуйте архив
3. Загрузите файлы в новый проект через редактор

---

## Часть 3: Восстановление базы данных

### 3.1. Выполните миграции
Скопируйте и запустите код ниже через чат с Юрой в НОВОМ проекте:

\`\`\`
Юра, выполни эти миграции для восстановления БД:

Миграция 1 - создание таблиц:
CREATE TABLE IF NOT EXISTS documents (
    id SERIAL PRIMARY KEY,
    name VARCHAR(500) NOT NULL,
    file_key VARCHAR(500) NOT NULL,
    size_bytes BIGINT NOT NULL,
    pages INTEGER,
    category VARCHAR(100),
    status VARCHAR(50) DEFAULT 'processing',
    uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    processed_at TIMESTAMP
);

CREATE TABLE IF NOT EXISTS document_chunks (
    id SERIAL PRIMARY KEY,
    document_id INTEGER NOT NULL,
    chunk_text TEXT NOT NULL,
    chunk_index INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    embedding_text TEXT
);

CREATE TABLE IF NOT EXISTS chat_messages (
    id SERIAL PRIMARY KEY,
    session_id VARCHAR(100) NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

Миграция 2 - создание индексов:
CREATE INDEX IF NOT EXISTS document_chunks_document_id_idx ON document_chunks(document_id);
\`\`\`

---

## Часть 4: Настройка секретов

### 4.1. Добавьте DEEPSEEK_API_KEY
В новом проекте скажите Юре:
\`\`\`
Добавь секрет DEEPSEEK_API_KEY для DeepSeek API
\`\`\`

Затем вставьте ваш API ключ в появившееся поле.

---

## Часть 5: Деплой backend функций

### 5.1. Разверните функции
В новом проекте скажите Юре:
\`\`\`
Задеплой все backend функции
\`\`\`

Это развернет 5 функций:
- chat (AI-чат с векторным поиском)
- upload-pdf (загрузка PDF в S3)
- process-pdf (извлечение текста и создание эмбеддингов)
- get-documents (получение списка документов)
- delete-pdf (удаление документов)

---

## Часть 6: Обновление URL функций

### 6.1. Обновите URL в коде
После деплоя Юра автоматически обновит файл \`src/components/hotel/types.ts\` с новыми URL функций.

Если этого не произошло, скажите:
\`\`\`
Обнови BACKEND_URLS в types.ts новыми URL из func2url.json
\`\`\`

---

## Часть 7: Тестирование

### 7.1. Проверьте функционал
1. **Админ-панель**: загрузите тестовый PDF документ
2. **Обработка**: дождитесь статуса "Готово"
3. **Чат**: задайте вопрос по содержимому документа
4. **Удаление**: попробуйте удалить документ

### 7.2. Проверьте базу данных
Скажите Юре:
\`\`\`
Покажи список всех документов в БД
\`\`\`

---

## ⚠️ Важные замечания

### Что копируется автоматически:
✅ Весь код (frontend + backend)
✅ Конфигурация проекта
✅ Структура компонентов

### Что НЕ копируется (нужно вручную):
❌ База данных (восстанавливается через миграции)
❌ Секреты (добавляются заново)
❌ Загруженные PDF файлы (загружаются заново)
❌ История чата (начинается с нуля)

---

## 🆘 Если что-то пошло не так

### Backend функции не работают:
\`\`\`
Юра, проверь логи backend функций
\`\`\`

### База данных не создалась:
\`\`\`
Юра, покажи структуру базы данных
\`\`\`

### Frontend не подключается к backend:
\`\`\`
Юра, проверь BACKEND_URLS в types.ts и func2url.json
\`\`\`

---

## 📊 Архитектура проекта

```
Frontend (React)
    ↓ HTTP
Backend Functions (Python)
    ↓ SQL
PostgreSQL Database
    ↓
- documents (метаданные PDF)
- document_chunks (текст + эмбеддинги)
- chat_messages (история чата)
```

**Векторный поиск:**
1. Вопрос → эмбеддинг через DeepSeek
2. Поиск похожих чанков (cosine similarity)
3. Топ-3 релевантных фрагмента → контекст для AI
4. DeepSeek генерирует ответ на основе контекста

---

## ✅ Готово!

После выполнения всех шагов у вас будет полная копия проекта со всем функционалом:
- AI-чат консьерж
- Загрузка и обработка PDF
- Векторный поиск по документам
- Админ-панель для управления базой знаний
